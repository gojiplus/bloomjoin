% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{bloom_params}
\alias{bloom_params}
\title{Report the Bloom filter chosen for a target false positive rate}
\usage{
bloom_params(n, p = 0.01)
}
\arguments{
\item{n}{Numeric scalar (> 0): expected number of distinct keys to insert.
Fractional values are allowed (estimates).}

\item{p}{Numeric scalar in (0, 1): target false positive rate.}
}
\value{
A list with class "bloom_params" containing:
\itemize{
\item n: input n
\item p_target: target p
\item m_bits: total bits, a power of two
\item bytes: total bytes
\item bits_per_key: m_bits / n
\item k: number of hash functions
\item fpr_est: the rate that (m, n, k) achieves
}
}
\description{
Given an expected number of distinct keys \code{n} and a target false positive rate
\code{p}, report the filter \code{\link[=bloom_join]{bloom_join()}} will build: total bits \code{m}, number of
hash functions \code{k}, and the false positive rate that combination achieves.
}
\details{
The numbers come from the same C++ routine the filter itself uses, so this
cannot describe a filter the package does not build. It used to: it advertised
a blocked layout, with a \code{block_bits} argument and a \code{blocks} count, that the
filter has never implemented, and reported an \code{m} and \code{k} the filter did not
use.

Sizing is the standard one (Broder & Mitzenmacher), as used by Guava's
\code{BloomFilter.create} and Spark's:
bits_per_key = -log(p) / (log(2)^2)
k            = log(2) * (m / n)
fpr(m, n, k) = (1 - exp(-k * n / m))^k

\code{m} is then rounded up to a power of two, because the filter indexes with a
bit mask rather than a modulo, and \code{k} is taken from the rounded \code{m}. Rounding
only ever adds bits, so the achieved rate lands at or under the request.
}
\examples{
bp <- bloom_params(1e6, 1e-2)
bp
}
