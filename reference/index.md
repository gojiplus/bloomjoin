# Package index

## BloomJoin Functions

Core functionality of the package

- [`bloomjoin-package`](bloomjoin.md) [`bloomjoin`](bloomjoin.md) :
  BloomJoin: Efficient data.frame joins using probabilistic prefilters
- [`bloom_join()`](bloom_join.md) : Join two data frames using a Bloom
  filter pre-filter
- [`bloom_params()`](bloom_params.md) : Report the Bloom filter chosen
  for a target false positive rate

## Helper Functions

Supporting functions

- [`bloom_join()`](bloom_join.md) : Join two data frames using a Bloom
  filter pre-filter
- [`bloom_params()`](bloom_params.md) : Report the Bloom filter chosen
  for a target false positive rate
- [`bloomjoin-package`](bloomjoin.md) [`bloomjoin`](bloomjoin.md) :
  BloomJoin: Efficient data.frame joins using probabilistic prefilters
- [`hash_keys32()`](hash_keys32.md) : Hash composite join keys to 32-bit
  integers (stable & type-aware)
